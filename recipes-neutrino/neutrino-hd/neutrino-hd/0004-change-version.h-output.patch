From a11eec57fbe26db6b4a83af79a158ba484187485 Mon Sep 17 00:00:00 2001
From: TheDuke
Date: Wed, 3 Dec 2014 09:32:11 +0100
Subject: [PATCH 847/847] change version.h output

Signed-off-by: Markus Volk <f_l_k@gmx.net>
---
 src/gui/Makefile.am | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/gui/Makefile.am b/src/gui/Makefile.am
index 8b78680..4cf93fe 100644
--- a/src/gui/Makefile.am
+++ b/src/gui/Makefile.am
@@ -6,7 +6,7 @@ version.h:
 	echo '#define BUILT_DATE "'`date`'"' > $@
 	@if test -d $(top_srcdir)/.git ; then \
 		pushd $(top_srcdir) ; \
-		GITDESCRIBE=$$(git describe --always --dirty || echo 'VCS failed') ; \
+		GITDESCRIBE=$$(git log --pretty=format:'%d %h %ad' --date=short -1 || echo 'VCS failed') ; \
 		popd ; \
 		echo '#define VCS "'$${GITDESCRIBE}'"' >> $@ ; \
 	fi
-- 
2.0.0.rc2

